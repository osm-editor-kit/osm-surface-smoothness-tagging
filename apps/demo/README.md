# demo

Standalone Vite app to exercise the surface/smoothness data + id-field without
running the full iD editor: mock iD adapters, a surface context selector, and a
live panel showing the resulting `{ surface, smoothness }` tags.

**Status:** scaffold only — see `../../WORK_PLAN.md` Phase 4.
